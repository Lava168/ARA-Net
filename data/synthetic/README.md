# Synthetic public tables

Place de-identified / synthetic CSV or NIfTI smoke assets here for local demos.

Restricted cohort MRI must stay outside the public repository. The default
smoke path uses `data/example_metadata.csv` + `scripts/prepare_features.py`.
